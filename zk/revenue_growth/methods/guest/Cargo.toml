[package]
name = "revenue-growth-guest"
version = "0.1.0"
edition = "2021"

[workspace]

[dependencies]
revenue-growth-core = { path = "../../shared" }
risc0-zkvm = { version = "=3.0.6", default-features = false, features = ["std"] }

[profile.release]
codegen-units = 1
debug = 0
lto = true
strip = "symbols"
