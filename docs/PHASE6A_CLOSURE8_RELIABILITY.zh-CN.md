# Phase 6A Closure-8 可靠性契约

[English](PHASE6A_CLOSURE8_RELIABILITY.md)

重大风险子任务仍为 REQUIRED，主 Risk 成功不能代替子任务。必要输出缺失仍阻断
Review、Proof、Report 和 Release。历史失败 Run 和已发布标签保持不可变。

`risk_follow_up` 按预先授权的 Sol → Luna → Terra → MiMo 顺序恢复。注册凭证权限与
模型执行策略仍必须满足，供应商响应不能自行增加授权。未知能力路线需执行受治理的
精确输入能力检查；检查成功并完成验证后直接消费结果，不重复调用。检查计入实际尝试，
上限为四次请求、两次同供应商模型切换、一次跨供应商切换、三次能力检查。任务总截止
时间仍为 300 秒，不提高其他任务默认预算和传输超时。供应商截止时间失败仅在剩余
权限与预算内可恢复；任务总截止时间耗尽后停止。传输失败本身不证明模型语义能力不合格。

专用上下文保留精确父输出及其制品哈希、重大风险问题、选中的已接受事实、成功计算值
与公式及持久化引用，移除无关叙述和恢复历史。父输出没有 Claim 级引用，不补造引用。
计算输入血缘保存在原始计算记录及完整 AgentOutput 输入引用中，不在提示词重复复制
标识列表。只有引用而没有提供事实不代表获得额外知识；未解决的证据缺口必须明确披露。

历史失败的离线重建中，消息文本从 87,636 缩减为 35,580 UTF-8 字节，同时恢复了
18 条证据正文与 10 条成功计算引用。这不是实测 HTTP 字节数或计费 token 数。
历史回放仍以 `REQUIRED_RESEARCH_OUTPUT_MISSING` 失败。离线通过不代表实跑验收通过。
