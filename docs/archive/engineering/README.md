# Engineering archive

The 29 former root-level PARALLEL_EXECUTION_STATUS* and WORKSTREAM_REPORT* files, plus the original package MANIFEST.md and SHA256SUMS.txt (31 files total), are preserved here with Git rename history. They are historical engineering evidence, not current product status or evaluator instructions. The manifest/checksums describe the original package, not the current checkout.

Start with the [product overview](../../product/PRODUCT_OVERVIEW.md) and [quickstart](../../deployment/EVALUATOR_QUICKSTART.md). Historical commands may describe obsolete environments; machine-specific repository prefixes are represented as <repository-root>. Read them as records, not instructions to execute against a current database.

Detailed domain/acceptance histories elsewhere in docs/integration and acceptance remain where stable references already exist; this is a minimal cleanup, not a wholesale history rewrite.
