{
  "name": "verifiable-financial-agent-system",
  "private": true,
  "version": "0.1.0",
  "engines": {
    "node": ">=24 <25"
  },
  "scripts": {
    "check:runtime": "node -e \"const major=Number(process.versions.node.split('.')[0]); if (major !== 24) { console.error('Node.js 24.x is required'); process.exit(1) } console.log(process.version)\" && npm --version"
  }
}
